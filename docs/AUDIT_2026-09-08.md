# Incentifi system audit — 2026-09-08 (read-only)

Scope: the legible pool + LossRewardPoolV2 rollout as deployed on Robinhood Chain (4663), the services around it, the database, the production frontend, tests, security posture, acceptance gate and docs. **Nothing was changed, sent or deployed.** Every on-chain figure was read live between blocks 57 582 404 and 57 588 227 (2026-09-08 09:26–09:40 UTC); the evidence files (chain.json, db.json, event scan, test logs, headless-browser DOM/console dumps) are in the session scratch folder.

Severity: **Critical** = fix before real users; **Should-fix** = fix within the 30-day window; **Note** = record, no action required now.

## 0. Findings ranked

| # | Severity | Finding | Where |
|---|---|---|---|
| 1 | **Critical** | One hot software key is owner **and** operator of everything: `hook.owner()`, `hook.deployer()`, `V2.owner()`, `V2.operator()`, `V1.owner()`, `V1.operator()` are all `0x78a4E4BCC8ab559B6d3B1Cb9eab0A04a2411c726`, and that same key is `OPERATOR_PRIVATE_KEY` on Railway (`scripts/loss-reward-worker.mjs:99`). A Railway env leak gives an attacker every power in §G: re-point deposits, set a malicious swap route, and publish arbitrary Merkle roots that allocate the pools' ETH to themselves. | on-chain; `scripts/loss-reward-worker.mjs:99` |
| 2 | **Critical** | Operator wallet runway: `0x78a4…` holds **0.00953 ETH**. Measured burn: one epoch publish = 79 812 gas ≈ 0.0000207 ETH; one collect ≈ 179 k gas ≈ 0.000046 ETH; at the 5-minute cadence a single continuously-underwater token costs ≈ 0.006 ETH/day → **≈ 1.5 days** of runway with one active token, less with more. There is **no low-balance alert** in the worker (`sendAlert` fires only for the freshness gate and fee-collection failures; no `getBalance` anywhere in `scripts/loss-reward-worker.mjs`). An empty operator means silent no-row epochs (see memory `loss-reward-operator-wallet`). | `0x78a4E4BC…`; `scripts/loss-reward-worker.mjs:306` |
| 3 | **Should-fix** | The worker only collects when **ETH** fees clear 10× gas (`scripts/loss-reward-worker.mjs:1279`): token-side-only fees (after sells) are never collected. Live: TESSSS has **2 410 779 tokens** (≈ 0.0048 ETH at the current price, ≈ 100× gas) sitting uncollected with `ethFees = 0`; the converter is idle. Value the token side with `checkpointEthValue` in the collect decision. | `scripts/loss-reward-worker.mjs:1279`, `:1302` |
| 4 | **Should-fix** | Verification gaps: **V2** `0x5d94246C…`, **converter** `0xe1BB0667…` and **V1** `0x697BDA9d…` are **not verified on Blockscout** (v1 API returns empty source; v2 API returns nothing). Sourcify: hook, factory, converter, V2, swapper = `exact_match`; **V1 is on neither**. Users clicking Blockscout links to V2/converter/V1 see unverified bytecode. | Blockscout |
| 5 | **Should-fix** | `tokens` table now holds **one row (TESSSS)**. Four other tokens the worker was still processing until 09:09 UTC today (`0x5b01…` = legible "111", `0x0fda…`, `0xfc57…`, `0x74bb…`) were removed; their `reward_epochs` (1 851 rows), `holder_cost_basis` and on-chain balances are orphaned. Consequences: V1's remaining **0.02236 ETH** belongs to tokens the worker no longer iterates (stranded by deletion, not by design); legible "111" has 0.0000257 ETH + 12 606 tokens uncollected forever; SMK95868 leaves 0.000149 ETH on V1. Deleting a `tokens` row is now a funds decision. | Supabase `tokens`, `reward_epochs`; V1 `getUnallocatedBalance` |
| 6 | **Should-fix** | Token page console errors on production: `Could not find the table 'public.token_chat_messages'` (repeats every poll; `src/lib/chat.ts:13`), and `Failed to load pool trade history: Wallet provider not available` (history reads go through `window.ethereum` instead of the public client; `src/lib/poolHistory.ts:30`), so visitors without a wallet get no trade history. | `src/lib/chat.ts:13`, `src/lib/poolHistory.ts:30` |
| 7 | **Should-fix** | Two thin routes will fall back at claim time: **QUBT** pool has **no 30 m and no 10 m TWAP right now** (`observe` reverts → `ReferenceUnavailable` → ETH); **BE** holds 0.98 WETH (a 0.05 ETH claim moves the price > 3 % → `BelowProtocolBound` → ETH). Both were above the generator's bar when the file was cut; liquidity moved. Re-run the census before advertising these two. | V2 routes QUBT `0xb0b255ec…`, BE `0xe3ECA0Fa…` |
| 8 | **Should-fix** | Test-harness drift (product is fine): `test/integration-layer.test.mjs:35-38` still expects the pre-rewire V3 factory `0x9fcea653…` (code: `0xa0143de8…`); `test/indexer-v4-discovery-retry.test.mjs` fake RPC has no `eth_call`, so the PR #21 legible-factory discovery read fails before the 5 000-block chunk commits (`:96`); `test/v4-trade-replay.test.mjs` (script `test:v4-replay`) expects the indexer not to touch `tokens`, but PR #21 tags `tokens.hook_address`. `npm test` therefore stops at the first failure and never reaches the 16 suites after it (all of which pass individually). | those files |
| 9 | **Should-fix** | `tsc` still fails on `src/lib/permit2.ts:76`, `:84`, `:152` (viem call-parameter typing, `authorizationList`); the app builds because Vite does not type-check. Either fix the call typing or the build will keep hiding new type errors behind these. | `src/lib/permit2.ts` |
| 10 | **Should-fix** | Docs are stale in 11 places (§I) — most importantly both design docs still call the owner a **hardware-wallet EOA** and the V2 doc's §B6 site table prescribes `LOSS_REWARD_POOL_LEGACY*` env vars that were never implemented; `INTEGRATION.md:28` lists only V1. | `docs/*`, `INTEGRATION.md` |
| 11 | **Should-fix** | Runbook `Preflight` and `DeployDryRun` **fail by design now** (`hook.lossRewardPool()` must be V1: `scripts/ops/Deploy-LossRewardV2.ps1:9`, Preflight's "already re-pointed? stop" check). `ReadBack` / `Routes*` still work. Re-scope Preflight for the post-re-point world before the next route refresh. | `scripts/ops/Deploy-LossRewardV2.ps1` |
| 12 | Note | Full `forge test` in one process: 45/49 pass; the 4 failures (`ConfigureStockRoutes`, `LegiblePool GraduationAcrossTwoTrades`, `LegiblePoolReview Review4`, `LossRewardPoolV2 ProtocolBoundBinding`) all **pass when run alone** — the parallel run saturates the public RPC (429s surface as `EvmError: Revert` on the fork). Run suites sequentially or against a private RPC. | `forge-test.log`, `forge-rerun.log` |
| 13 | Note | TESSSS epoch **101 is `pending_funding`** (0.00074 ETH) because V2's unallocated balance is 0.000129 ETH after the 33 published epochs were fully claimed. Expected behaviour; it funds itself on the next collect. But the DB `token_market_snapshots_evm.loss_pool_tvl_eth` is still 0 for TESSSS (the snapshot writer does not know V2), so anything reading that column is wrong. | `reward_epochs` epoch 101; `token_market_snapshots_evm` |
| 14 | Note | Fallback monitor: 3 × `RewardPaidInEthFallback(BelowMinimum)` since deploy (claims of 0.00015 / 0.00014 / 0.00137 ETH) — expected, not alerted. **Two real GOOGL payouts** happened: 0.00238 ETH → 0.01748 GOOGL and 0.00275 ETH → 0.02022 GOOGL (raw) to `0xd2dF2A28…`. No `SwapFailed` / `BelowProtocolBound` / `ReferenceUnavailable`. | V2 events blocks 57 556 923 – 57 577 966 |
| 15 | Note | Token page shows "Trading Venue: Incentifi Bonding Curve" for a legible token pre-graduation (`src/pages/token-preview/page.tsx:1711` only says "Uniswap V4 Pool" once graduated). Arguably right (the curve is the pool), but it contradicts the "legible" story. | `page.tsx:1711` |
| 16 | Note | Headless Edge: `https://api.coinbase.com/v2/prices/ETH-USD/spot` fetch fails (`Failed to fetch`) on the token page; likely tracking-prevention in headless mode — confirm in a normal browser (`page.tsx:302`). Market-cap figures fall back to the reference price. | `page.tsx:302` |
| 17 | Note | GMGN and Axiom cannot be probed programmatically (Cloudflare "Just a moment" / HTTP 403). DexScreener indexes both tokens (§H). | — |
| 18 | Note | Vercel / Railway / Supabase secret values could not be read (no CLI auth in this environment). Parity below is **inferred from behaviour** and marked as such. | §B |

## A. On-chain state (live)

### A1. Contracts, owners, pointers, constants

| Contract | Address | Read back | Matches design docs |
|---|---|---|---|
| Hook `IncentifiV4LegibleHook` | `0x921d0bE20A21e5A687734b4dF6302EA55BD168C0` (17 446 B) | owner = deployer = `0x78a4…`; factory `0xD4ce…`; feeConverter `0xe1BB…`; **lossRewardPool `0x5d94…` (V2)**; poolManager `0x8366…`; `PRE/DEFAULT/MAX_POST_GRADUATION_FEE_PIPS` = 20 000 / 20 000 / 20 000; `TICK_LOWER/UPPER/SPACING` = 174070 / 200310 / 10; `CURVE_LIQUIDITY` 48 215 215 764 839 215 328 822 | ✅ (§12 V4 doc, §C10 V2 doc) |
| Factory `IncentifiV4LegibleFactory` | `0xD4ce8F9577F3a865C3bA0c9d156f2b615df89Dda` (2 945 B) | hook `0x921d…`; `POOL_FEE` 8 388 608 (dynamic-fee flag); no `owner()` (by design) | ✅ |
| Converter `IncentifiFeeConverter` | `0xe1BB0667d64683072BaeE03D8D9Feb201dcAF7D9` (3 811 B) | hook `0x921d…`; **lossRewardPool() → `0x5d94…`** (reads the hook); poolManager `0x8366…`; `MAX_SLIPPAGE_BPS` 300 | ✅ |
| V2 `LossRewardPoolV2` | `0x5d94246CD31064Da02E953DB357F0001F0E9A631` (11 609 B) | owner = operator = `0x78a4…`; stockFactory `0x4783…`; accessRegistry `0xe10b…`; `minStockRewardWei` 2 000 000 000 000 000; `MAX_DEVIATION_BPS_CAP` 2 000; `MIN_TWAP_WINDOW` 300; `assetSetters(factory)` **true**; `assetSetters(owner)` false | ✅ |
| Swapper `RewardSwapperUniswapV3` | `0xEDe37d70Ca99E25D501c780D6Ed24307C63A3aDe` (4 896 B) | lossRewardPool `0x5d94…`; weth `0x0Bd7…`; v3Factory `0x1f7d…`; ETH balance 0 | ✅ |
| V1 `LossRewardPool` | `0x697BDA9db5a297a9Cd9ED969BBF2549d0527DcdF` (3 789 B) | owner = operator = `0x78a4…`; contract ETH 0.022361 | ✅ |

Access registry `paused()` = false. Gas price 0.256 gwei. Chain id 4663.

### A2. The 26 routes (all present, all selectable, all canonical)

Every `assetRoute(asset)` on V2 equals `config/loss-reward-stock-routes.json` (swapper `0xEDe3…`, pool, fee, `twapWindow` 1800, `maxDeviationBps` 300, enabled); `isSelectableAsset` = true for all 26; `v3Factory.getPool(WETH, asset, fee)` == route pool for all 26; `swapper.validateRoute` = true for all 26; no asset paused. Depth **now** (WETH in the route pool; TWAP availability = `observe([1800,0])` / `observe([600,0])` succeed):

| Symbol | Fee | WETH in pool | Asset in pool | TWAP 30 m / 10 m |
|---|---|---|---|---|
| SPY | 500 | 300.38 | 1 400 | ✅ / ✅ |
| NVDA | 500 | 232.92 | 2 479 | ✅ / ✅ |
| GLD | 10000 | 140.89 | 941 | ✅ / ✅ |
| SPCX | 500 | 138.06 | 1 918 | ✅ / ✅ |
| MSTR | 10000 | 133.39 | 2 313 | ✅ / ✅ |
| HIMS | 10000 | 93.17 | 612 | ✅ / ✅ |
| TSLA | 3000 | 74.73 | 791 | ✅ / ✅ |
| COIN | 3000 | 69.73 | 1 998 | ✅ / ✅ |
| CRCL | 10000 | 39.72 | 924 | ✅ / ✅ |
| RBLX | 3000 | 38.00 | 2 029 | ✅ / ✅ |
| AAPL | 500 | 34.96 | 594 | ✅ / ✅ |
| QQQ | 3000 | 31.86 | 119 | ✅ / ✅ |
| MU | 10000 | 23.91 | 35 | ✅ / ✅ |
| LLY | 3000 | 22.96 | 50 | ✅ / ✅ |
| SGOV | 10000 | 20.97 | 285 | ✅ / ✅ |
| META | 3000 | 20.87 | 68 | ✅ / ✅ |
| SNDK | 3000 | 19.30 | 12 | ✅ / ✅ |
| TSM | 3000 | 9.99 | 38 | ✅ / ✅ |
| GME | 500 | 7.14 | 2 288 | ✅ / ✅ |
| GOOGL | 100 | 5.80 | 94 | ✅ / ✅ |
| COST | 10000 | 2.79 | 12 | ✅ / ✅ |
| MRVL | 3000 | 2.38 | 39 | ❌ / ✅ (falls to the 10 m window) |
| BB | 10000 | 2.07 | 512 | ✅ / ✅ |
| DJT | 500 | 1.84 | 629 | ✅ / ✅ |
| **QUBT** | 10000 | 1.59 | 196 | **❌ / ❌ → every QUBT claim pays ETH (ReferenceUnavailable) until the ring refills** |
| **BE** | 3000 | 0.98 | 4.6 | ✅ / ✅ but too thin for the 3 % bound above ~0.03 ETH |

### A3. Every legible token launched so far (4 `TokenLaunched` events on the factory)

| Token | Symbol | Creator | Reward asset | Uncollected in position | `creatorBalances(creator)` | V2 unallocated / deposited / allocated / claimed | V1 unallocated | Converter pending |
|---|---|---|---|---|---|---|---|---|
| `0x6497226f…49A2` (blk 56 940 573) | SMK94829 (smoke 1) | `0x76E86e27…` | ETH (unset) | 0 / 0 | 0.000149 ETH | 0 / 0 / 0 / 0 | 0 | 0 |
| `0xC517a323…E725` (blk 56 950 847) | SMK95868 (smoke 2) | `0x76E86e27…` | ETH (unset) | 0 / 0 | (same creator) | 0 / 0 / 0 / 0 | **0.000149 ETH (pre-re-point; token not in `tokens` → never drained)** | 0 |
| `0x5B017598…50Cc` (blk 57 027 846) | "111" | `0xba69Ca72…` | ETH (unset) | **0.0000257 ETH + 12 606 tokens** | 0.004033 ETH | 0 / 0 / 0 / 0 | 0 | 0 |
| `0x8976d274…d38B` (blk 57 273 558) | TESSSS | `0xba69Ca72…` | **GOOGL** `0x2e0847E8…` (set, not forced) | **0 ETH + 2 410 779 tokens** | (same creator, 0.004033 ETH) | 0.000129 / 0.006920 / 0.006792 / 0.006792 | 0 | 0 |

`postGraduationFeePips` = 20 000 for all four (2 %). V2 contract balance 0.0001286 ETH == `totalDeposited − totalClaimed` for TESSSS (the only V2-funded token) — accounting consistent.

### A4. V1 has not grown since the re-point

Last V1 log is at block **57 114 912** (`RewardDeposited`, tx `0x97cf656a…`), before the re-point block 57 150 278; no V1 events after it. V1 contract balance 0.022361 ETH (the design doc's "0.0222 ETH" + the SMK95868 deposit). The archive read of `totalDeposited` at block 57 150 278 is not supported by the public RPC ("Missing or invalid parameters"); the log evidence stands on its own. **V1.totalDeposited did not grow. ✅**

### A5. Source verification

| Contract | Sourcify (chain 4663) | Blockscout |
|---|---|---|
| Hook | exact_match (creation + runtime), 2026-09-07T14:45:42Z | verified, full match |
| Factory | exact_match, 14:54:08Z | verified, full match |
| Converter | exact_match, 14:54:11Z | **not verified** (v1 API: empty source) |
| V2 | exact_match, 2026-09-07T21:06:16Z | **not verified** |
| Swapper | exact_match, 21:06:16Z | verified, full match |
| V1 | **not on Sourcify** | **not verified** |

### A6. Owner/operator wallet and burn rate

`0x78a4…`: **0.009525 ETH**, nonce 359. Measured today: epoch publish 79 812 gas (tx `0x019b0d24…`, 0.0000207 ETH at 0.259 gwei); `collect(TESSSS)` estimate 178 993 gas (0.000046 ETH); convert ≈ 0.00005–0.00008 ETH. Worker cadence 5 min → up to 288 publishes/day per token with underwater holders (≈ 0.006 ETH/day), plus collect/convert whenever fees ≥ 10× gas (bounded at ≤ 10 % of fee flow). **Runway ≈ 1.5 days for one active token; < 1 day with three.** Top up and add a balance alert (finding 2).

## B. Config parity (Vercel · Railway · Supabase · in-code fallbacks)

I have no CLI access to Vercel, Railway or Supabase secrets here; the platform columns are **inferred from live behaviour** (bundle `index-C9JD64PE.js`, on-chain transactions from the operator, gateway responses). Rows marked ⚠ could not be confirmed.

| Variable | In-code fallback | Vercel (bundle) | Railway worker | Railway indexer | Supabase gateway |
|---|---|---|---|---|---|
| Legible hook / factory / converter | `0x921d…` / `0xD4ce…` / `0xe1BB…` (`uniswapAddresses.ts:120-128`, `legiblePool.mjs:11-13`) | present in bundle ✅ | tags `tokens.hook_address` = `0x921d…` ✅ | TESSSS trades indexed with the tag ✅ | n/a |
| `LOSS_REWARD_POOL_V2` / `_ADDRESS` | **none** (fail-closed) | `0x5d94…` present; badge reads GOOGL ✅ | epochs published on V2 by `0x78a4…` (33) ✅ | n/a | `/query` needs a session; V2 rows are served with `pool_address` from the DB, and frontend claims on V2 succeeded (33 claims) — gateway env ⚠ not directly confirmable |
| `VITE_LEGIBLE_LAUNCH_ENABLED` | `false` | **true** (launch page shows the legible dropdown; TESSSS launched legible) | — | — | — |
| `VITE_STOCK_REWARDS_ENABLED` | `false` | **true** (26 stocks in the dropdown) | — | — | — |
| `VITE_LOSS_REWARD_POOL` (V1) | `0x697b…` (`uniswapAddresses.ts:32`) — correct: V1 claims + legacy TVL | 6 occurrences of V1 in bundle (V1 claim path, TVL fallback) ✅ | worker `VITE_LOSS_REWARD_POOL` fallback V1 (`worker:102`) ✅ | indexer `:118` V1 ✅ | `index.ts:44` V1 default ✅ (V1 claim plan) |
| `VITE_ROBINHOOD_ASSETS_PROXY_URL` | derived from `VITE_SUPABASE_URL` | `loss-reward-gateway/assets` in bundle ✅; `GET /assets` → 200 ✅ | — | — | deployed post-#25 ✅ |
| `FEE_COLLECT_*` | enabled / 10× / 24 h | — | active: collects at 08:34, 08:40, 08:49 from `0x78a4…` ✅ | — | — |
| `OPERATOR_PRIVATE_KEY` | — | — | **= owner key** (finding 1) | — | — |
| GenericSell trio `0xC5Ef…` / `0x4166…` / `0x762b…` | still in code (`uniswapAddresses.ts:61-72`, `evm-indexer.mjs:125`) | present ✅ — needed for pre-legible tokens; not dead | dual discovery ✅ | dual discovery ✅ | — |
| `LOSS_REWARD_POOL_LEGACY*` (doc §B6) | **never implemented** — the doc's env names do not exist in code | — | — | — | — |

Leftover / unread variables: `VITE_INCENTIFI_SWAP_ROUTER`, `VITE_UNISWAP_SWAP_ROUTER`, `VITE_UNISWAP_POSITION_MANAGER` are read (V3 paths, `addLiquidity.ts`, `swap.ts`) — not dead. The design doc's "25 sites" list (`docs/LOSS_REWARD_ASSET_DESIGN.md:233-247`) was superseded by the dual-pool design: V1 stays hardcoded on purpose at the four runtime sites (V1 claims, legacy drain), V2 has no fallback anywhere. No site still points production traffic at V1 for new deposits (hook + converter read V2).

## C. Services (live probes; Railway logs not accessible)

- **Indexer**: heartbeat `evm-indexer` status **ok**, "Indexed through block 57578956", updated 09:21:09 UTC (12 s old at read time). TESSSS trades (buys, sells, dust sells) land in `token_trades_evm` with `block_time`; `tokens.hook_address` tagged. Both factories ingesting: TESSSS (legible) indexed; the GenericSell path has no live tokens left in `tokens`. RPC errors: not visible without logs ⚠.
- **Worker**: dual-pool selection evident from data — 33 TESSSS epochs published on **V2** with tx hashes (first 08:39, last 09:14 UTC), 62 `completed_dust`, 6 `completed_empty`, 1 `pending_funding` (epoch 101). Collect/convert activity from the operator at 08:34:15, 08:40:13, 08:49:25 (collect) and 08:34:18, 08:40:15, 08:49:31 (convert) — PR #28 went live within minutes of merge. Fallback monitor: 3 BelowMinimum, nothing alertable. **Gap:** token-side-only fees not collected (finding 3).
- **Gateway**: `GET /assets` → 200; `POST /challenge` → 200 (nonce issued); `POST /query` without a session → 401 (correct). Claim-plan V1-vs-V2 on a real token: the 33 TESSSS claims were executed on V2 by the frontend using the gateway's `poolAddress` rows; the relayer path is unit-tested (`test/gateway-claim-plan.test.mjs`). A live `/query` needs a wallet signature, so it was not exercised (would write an `auth_nonces` row).

## D. Database

- Migrations applied: `reward_epochs.pool_address` present, **0 of 1 953 rows null**; `tokens.hook_address`, `tokens.reward_asset`, `tokens.reward_asset_symbol` present; TESSSS row: hook `0x921d…`, reward_asset `0x2e0847…` / GOOGL. ✅
- `pool_address` consistency: all 33 TESSSS `published` rows carry V2 and an `onchain_tx_hash`; V2 emitted exactly 33 `EpochRootPublished` (allocated 0.006791879570437093 ETH = DB sum). ✅ 608 V2-tagged rows = TESSSS 102 + `completed_empty` rows for four removed tokens.
- `tokens` rows with null `hook_address` that should have one: **0** — but only because the table has one row (finding 5). 4 legible launches on-chain vs 1 token row.
- `token_market_snapshots_evm.loss_pool_tvl_eth` = 0 for TESSSS while V2 has been funded (finding 13).
- `holder_cost_basis` for TESSSS: 3 rows, 1 eligible.

## E. Frontend (production `www.incentifi.fun`, bundle `index-C9JD64PE.js`, headless Edge)

- `/launch`: "Loss Reward Asset" `<select>` present with **ETH + all 26 stocks** (names shown, e.g. "GOOGL · Alphabet Class A"), default copy "Loss rewards for this token are paid in ETH". No asset-list error notice shown (the proxy answered). ✅
- `/token-preview/TESSSS`: "Payout Asset **Loss Reward: GOOGL**", "**Loss Pool Balance (V2, unallocated)**", live price/liquidity/24 h volume rendered. "≈ N GOOGL" / "Below 0.002 ETH — paid in ETH" and the Collect-then-Claim button only render for a connected wallet with claimable epochs — not reachable headless; both are covered by `test/hardhat/legible-fees-fork.test.ts` §3–§4 and the strings are in the bundle (`Collect & Claim`, `Accrued in pool`, `paid in ETH`). ⚠ not visually confirmed on production.
- Gas headroom: the bundle carries the `300000n` floor and two `eth_estimateGas` call sites — the headroom code is deployed. ✅
- Console on the token page: `token_chat_messages` missing (×5), pool trade history needs a wallet, Coinbase price fetch failed (findings 6, 16). Home and launch pages: only tracking-prevention notices (remixicon CSS), no app errors.
- Build: `vite build` passes; the only warning is the 9-month-old `caniuse-lite` (`npx update-browserslist-db@latest`).

## F. Tests & code health

| Suite | Result | Root cause when failing |
|---|---|---|
| `npm test` (27 files) | **stops at `indexer-v4-discovery-retry`** (exit 1) | fake RPC in the test has no `eth_call`; the PR #21 legible-factory discovery read (`isLaunched`) throws before the first 5 000-block chunk commits (`test/indexer-v4-discovery-retry.test.mjs:96`). Harness drift, not an indexer bug (the live indexer is healthy). |
| every npm test file run individually | 25 pass; `integration-layer` fails | expects the retired V3 factory `0x9fcea653…` (`:35-38`); code and INTEGRATION.md say `0xa0143de8…`. Stale expectation. |
| `test:v4-replay` (`v4-trade-replay.test.mjs`) | fails | "unexpected tables touched: … tokens": PR #21 made the indexer write `tokens.hook_address`. Stale expectation. |
| Hardhat forks: `legible-cutover-fork`, `loss-reward-v2-claims-fork`, `legible-fees-fork` | **3/3 pass** (sequential) | — |
| `forge test` (7 suites, 49 tests, parallel) | 45 pass, 4 fail with `EvmError: Revert` | all 4 pass individually (finding 12): public-RPC saturation under parallel forks |
| `tsc --noEmit -p tsconfig.app.json` | 3 errors, all `src/lib/permit2.ts` (76, 84, 152) | viem typing drift; pre-existing |
| Skipped tests | none | — |

Dead code / remnants: no timelock code remains (only the explanatory comment at `IncentifiV4LegibleHook.sol:268`); the GenericSell router constants are still used by live paths; `scripts/rewire-v3-factory-router.mjs` is historical; the doc-only `LOSS_REWARD_POOL_LEGACY*` names never existed in code. `supabase/.temp/` and `broadcast/DeployLossRewardPoolV2.s.sol/4663/dry-run/` sit untracked in the working tree.

## G. Security posture

Owner **and** operator of every contract = the software key `0x78a4…` (Foundry keystore `incentifi-owner`, and the same key as Railway's `OPERATOR_PRIVATE_KEY`). Privileged surface and blast radius:

| Contract | Function | Who | Blast radius |
|---|---|---|---|
| Hook | `transferOwnership` (`:260`) | owner | hands every hook power to a new key |
| Hook | `setLossRewardPool` (`:271`) | owner | **redirects every future loss-pool deposit** (must be a contract; no timelock) |
| Hook | `setPostGraduationFee` (`:282`) | owner | 0 … 2 % per token; **cannot exceed `MAX_POST_GRADUATION_FEE_PIPS` = 20 000 (`FeeTooHigh`)**, and pre-graduation fee is a constant — raising fees is impossible ✅ |
| Hook | `setLpOpen` (`:290`) | owner | opens external LP on a graduated pool |
| Hook | `setFactory` / `setFeeConverter` (`:246`, `:253`) | deployer, **one-time** (`*AlreadySet`) — already set, inert ✅ |
| Hook | `collect`, `graduate`, `seedCurve` | anyone | permissionless plumbing, no value extraction |
| Factory | `launchToken` | anyone | launches; no owner |
| Converter | `convert` | anyone | sells pending fees at ≥ checkpoint − 3 %; no owner |
| V2 | `setAssetRoute` (`:355`) | owner | **can point an asset at an arbitrary `swapper` contract** → a malicious adapter could keep the claimant's ETH on `claim*As` (the pool trusts the adapter's report); mitigated only by the owner's honesty |
| V2 | `setAssetEnabled`, `setAssetSetter`, `setMinStockReward`, `forceEthPayout` (`:366-393`, `:337`) | owner | disable/enable routes, authorise setters, change the min, force ETH; none moves value |
| V2 | `setOperator`, `transferOwnership` (`:404`, `:410`) | owner | key rotation |
| V2 / V1 | `setEpochMerkleRoot` (`V2:147`, `V1:145`) | **operator (or owner)** | **publishes any root for any allocation ≤ unallocated → the operator can allocate the pool's ETH to itself.** This is the only withdrawal path in either pool. |
| V1 | `setOperator`, `transferOwnership` (`:235`, `:241`) | owner | key rotation |
| Swapper | `swap` (`:95`) | pool only | stateless; holds nothing (balance 0) |

No `withdraw`, `sweep`, `selfdestruct` or `delegatecall` in V1, V2 or the swapper; value leaves the pools only through claims (`V1:181,210`, `V2:321`). Fee cap confirmed on-chain (`MAX = DEFAULT = PRE = 20 000`). **Because operator == owner == the Railway hot key, "nothing can withdraw" is not true in practice** — see finding 1 and the follow-ups.

## H. Acceptance gate (aggregators)

| Token | DexScreener | GMGN | Axiom |
|---|---|---|---|
| SMK95868 `0xC517a323…` | **indexed**: chain `robinhood`, dex `uniswap`, label `v4`, pair `0x7000b77f…`, liquidity $3 909, 24 h volume $36.82 | Cloudflare challenge — not verifiable programmatically | HTTP 403 — not verifiable |
| TESSSS `0x8976d274…` | **indexed**: pair `0xab01f227…`, liquidity $3 926, 24 h volume $2 376.9; search "TESSSS" returns it | same | same |

## I. Runbook & docs — stale statements

1. `docs/V4_LEGIBLE_POOL_DESIGN.md:3` "NOT deployed" — deployed 2026-09-07.
2. `docs/V4_LEGIBLE_POOL_DESIGN.md:96`, `:104`, `:141` "owner is a hardware-wallet EOA" — it is a software key in a Foundry keystore, also used as the worker operator.
3. `docs/V4_LEGIBLE_POOL_DESIGN.md:118` "re-point `uniswapAddresses.ts` + indexer factory default" — done via flags (PR #21), not defaults.
4. `docs/V4_LEGIBLE_POOL_DESIGN.md:141` "`hook.lossRewardPool() == converter.lossRewardPool() == 0x697B… (the V1 pool)`" — now V2.
5. `docs/V4_LEGIBLE_POOL_DESIGN.md:147` converter Blockscout "pending" — still unverified there; V2 and V1 also unverified.
6. `docs/V4_LEGIBLE_POOL_DESIGN.md:149` "Next (§5 acceptance …) only then re-point" — the smoke test, cutover and re-point all happened.
7. `docs/LOSS_REWARD_ASSET_DESIGN.md:3` "Phase C implemented in this PR … Nothing deployed" — deployed.
8. `docs/LOSS_REWARD_ASSET_DESIGN.md:226` "owner = hardware EOA" — see 2.
9. `docs/LOSS_REWARD_ASSET_DESIGN.md:233-247` the "every place V1 is hardcoded" table and the `LOSS_REWARD_POOL_LEGACY*` env plan — superseded by the dual-pool design; those env names never existed.
10. `docs/LOSS_REWARD_ASSET_DESIGN.md:380` "Routes live at the time of writing: AAPL / TSLA / NVDA only" — 26 live since 505d2d5.
11. `scripts/ops/Deploy-LossRewardV2.ps1:9` (Preflight "must still be V1") and the Preflight/DeployDryRun stop conditions — obsolete post re-point. `INTEGRATION.md:28` lists only the V1 pool and `:47/:59` describe the 2 % fee as a router/curve fee — the legible hook enforces it in-pool.

## 1. Must fix before real users

1. **Split the keys and move the owner to a hardware wallet** (finding 1): create a fresh operator key for Railway; `V1.setOperator`, `V2.setOperator`; then `transferOwnership` on hook, V2 and V1 to a hardware wallet; rotate/retire the `incentifi-owner` keystore password. Until then the whole system is one env leak away from total loss of pool funds and deposit redirection.
2. **Fund the operator and alert on balance** (finding 2): top up to cover ≥ 30 days at the measured burn (≈ 0.2 ETH for one busy token; scale with tokens) and add a `getBalance` check with `sendAlert` to the worker tick.
3. **Collect token-side-only fees** (finding 3): include `checkpointEthValue(tokenFees)` in the collect decision so sells' fees do not sit idle (TESSSS: 2.41 M tokens now).
4. **Verify V2, converter and V1 on Blockscout** (finding 4) — users are being pointed at unverified addresses in the UI's explorer links.
5. **Decide the fate of the deleted tokens' funds** (finding 5) — 0.0224 ETH on V1 and the "111" fees are unreachable while those rows are gone; either restore the rows so the worker drains them or record the amounts as written off, and add a guard so `tokens` deletions are a conscious funds decision.
6. **Fix the token-page console errors** (finding 6) — create `token_chat_messages` or disable chat; read pool history through the public client.
7. **Refresh the route census** (finding 7) and disable QUBT (`setAssetEnabled(QUBT, false)`) unless its TWAP ring recovers; consider dropping BE.

## 2. 30-day maintenance checklist

- [ ] Weekly: operator balance ≥ 7 days of burn (worker alert once added); check `reward_epochs` for `pending_funding` rows older than one tick on any token with fee flow.
- [ ] Weekly: `node scripts/ops/enumerate-stock-venues.mjs` + `generate-stock-routes.mjs`; diff `config/loss-reward-stock-routes.json`; disable routes whose pool lost TWAP or fell below 1 WETH; `RoutesDryRun`/`RoutesBroadcast` only for additions.
- [ ] Weekly: `RewardPaidInEthFallback` breakdown (the worker alerts on non-expected reasons; confirm the webhook is wired — `ALERT_WEBHOOK_URL` could not be verified here).
- [ ] Weekly: indexer heartbeat age < 120 s; `tokens.hook_address` non-null for every legible launch; 4 on-chain launches vs `tokens` rows reconciled.
- [ ] After every worker deploy: confirm `[POOL SELECT]` / `[FEE COLLECT]` lines in Railway logs and that V1 has no new `RewardDeposited`.
- [ ] Monthly: Sourcify + Blockscout status of all six contracts; keystore password rotation; `npm test` sequence unblocked (fix the three stale harnesses) and `forge test` run per file.
- [ ] Monthly: re-read `minStockRewardWei` against gas (claimBatchAs ≈ 352 k gas): at 0.26 gwei the 0.002 ETH minimum is ≈ 22× the swap cost — fine; revisit if gas rises.
- [ ] Update the docs listed in §I and the runbook Preflight; add `docs/AUDIT_*.md` to the release checklist.

## 3. Open follow-ups

- **Two-hop adapter** (ETH → USDG → stock; new `IRewardSwapper`): ~40 more stocks with ≥ 1 000 USDG pools (WETH/USDG 0.01 % pool holds 6 416 WETH / 15.9 M USDG). Needs its own TWAP reference (product of two pools) and fork tests.
- **Token-order-agnostic adapter**: 7 stocks whose address sorts below WETH (VTI JNJ TTD AMC FLY SMH RDDT; AMC/RDDT have deep pools) — `validateRoute` and `swap` assume WETH = token0.
- **Hardware wallet + `transferOwnership`** on hook, V2, V1; separate operator key; **keystore password rotation** for `incentifi-owner` after the split.
- Worker: token-side fee valuation in the collect decision; operator-balance alert; `token_market_snapshots_evm.loss_pool_tvl_eth` from V2.
- Harness drift: `integration-layer` factory address, `indexer-v4-discovery-retry` fake RPC `eth_call`, `v4-trade-replay` table set; `permit2.ts` typing.
- Docs: §I list; INTEGRATION.md pool table; runbook Preflight for the post-re-point world.
